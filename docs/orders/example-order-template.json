{
  "order_id": "ORD-YYYY-MM-DD-###",
  "version": "2.0",

  "classification": {
    "type": "feature|bugfix|refactor|test|docs|infrastructure",
    "risk_level": "low|medium|high|critical",
    "estimated_complexity": "trivial|simple|moderate|complex",
    "requires_human_review": false,
    "affects_production": false
  },

  "context": {
    "problem_statement": "Short, single-sentence description of the issue or opportunity.",
    "goal": "Single sentence describing the desired end state.",
    "non_goals": [
      "Explicitly state what this order will NOT cover."
    ],
    "related_orders": ["ORD-2024-01-01-001"]
  },

  "execution": {
    "prerequisites": {
      "tools": ["uv", "pytest"],
      "environment": ["API_KEY", "DATABASE_URL"],
      "pre_checks": [
        "Tests on main branch are green",
        "No active incidents impacting this component"
      ]
    },
    "tasks": [
      {
        "id": "T1",
        "description": "Atomic task description the agent should execute.",
        "type": "automated|semi_automated|manual",
        "estimated_duration": "5m",
        "validation": "Assertion that proves the task succeeded.",
        "rollback": "Steps to undo the change if needed.",
        "depends_on": []
      },
      {
        "id": "T2",
        "description": "Follow-up task example.",
        "type": "automated",
        "estimated_duration": "10m",
        "validation": "Run test suite, expect all green.",
        "rollback": "git revert <commit>",
        "depends_on": ["T1"]
      }
    ],
    "checkpoints": [
      {
        "after_task": "T1",
        "verify": ["Ensure no errors in logs"],
        "pause_for_review": false
      }
    ]
  },

  "constraints": {
    "must": [
      "Maintain backward compatibility",
      "Update documentation if user-facing behaviour changes"
    ],
    "do_not": [
      "Do not modify production databases directly",
      "Do not commit secrets"
    ],
    "performance": {
      "max_execution_time": "30m",
      "max_memory_usage": "2GB",
      "max_api_calls": 100
    }
  },

  "validation": {
    "acceptance_criteria": [
      "Feature behaves as specified",
      "All existing tests pass"
    ],
    "testing": [
      "uv run python -m pytest tests/test_module.py"
    ],
    "post_checks": [
      "Monitor error rates for 15 minutes"
    ]
  },

  "metadata": {
    "created_at": "2025-02-15T10:00:00Z",
    "updated_at": "2025-02-15T10:00:00Z",
    "created_by": "agent@company.com",
    "status": "draft|approved|in_progress|completed|failed|rolled_back"
  },

  "audit": {
    "require_approval": true,
    "approvers": ["senior-engineer@company.com"],
    "escalation_triggers": [
      "Unexpected data loss",
      "Execution time exceeds 2x estimate"
    ],
    "fallback": "Rollback changes and notify stakeholders"
  }
}
